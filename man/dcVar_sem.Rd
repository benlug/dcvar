% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-sem.R
\name{dcVar_sem}
\alias{dcVar_sem}
\title{Fit a SEM copula VAR(1) model}
\usage{
dcVar_sem(
  data,
  indicators,
  J,
  lambda,
  sigma_e,
  time_var = "time",
  prior_mu_sd = 0.25,
  prior_phi_sd = 0.5,
  prior_sigma_sd = 0.5,
  prior_rho_sd = 0.75,
  chains = 4,
  iter_warmup = 2000,
  iter_sampling = 4000,
  adapt_delta = 0.95,
  max_treedepth = 13,
  seed = NULL,
  cores = NULL,
  refresh = 500,
  init = NULL,
  stan_file = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame with time series of indicator variables.}

\item{indicators}{A list of two character vectors, each naming J indicator
columns per latent variable. For example:
\code{list(PA = c("y1_1", "y1_2", "y1_3"), NA_ = c("y2_1", "y2_2", "y2_3"))}.}

\item{J}{Number of indicators per latent variable.}

\item{lambda}{Numeric vector of length J with fixed factor loadings.}

\item{sigma_e}{Fixed measurement error SD (scalar).}

\item{time_var}{Name of the time column (default: \code{"time"}).}

\item{prior_mu_sd}{Prior SD for intercepts: \code{mu ~ normal(0, prior_mu_sd)}.}

\item{prior_phi_sd}{Prior SD for VAR coefficients:
\code{phi ~ normal(0, prior_phi_sd)}.}

\item{prior_sigma_sd}{Prior SD for lognormal on innovation SDs:
\code{sigma ~ lognormal(0, prior_sigma_sd)}.}

\item{prior_rho_sd}{Prior SD for rho_raw:
\code{rho_raw ~ normal(0, prior_rho_sd)}, with \code{rho = 0.97 * tanh(rho_raw)}.}

\item{chains}{Number of MCMC chains.}

\item{iter_warmup}{Warmup iterations per chain.}

\item{iter_sampling}{Sampling iterations per chain.}

\item{adapt_delta}{Target acceptance rate.}

\item{max_treedepth}{Maximum tree depth.}

\item{seed}{Random seed.}

\item{cores}{Number of parallel chains.}

\item{refresh}{How often to print progress.}

\item{init}{Custom init function or \code{NULL}.}

\item{stan_file}{Custom Stan file path or \code{NULL}.}

\item{...}{Additional arguments passed to \code{cmdstanr::CmdStanModel$sample()}.}
}
\value{
A \code{dcVar_sem_fit} object.
}
\description{
Fits a copula VAR(1) model with a fixed measurement model (factor
loadings and measurement error SD are not estimated). Latent innovations
are treated as parameters, making this model computationally intensive
for large T.
}
\details{
\strong{Boundary constraints.} The SEM model constrains each VAR coefficient
(Phi) to the interval \eqn{[-0.99, 0.99]}, unlike other dcVar models
where Phi is unconstrained. Very strong autoregressive or cross-lag
dynamics near \eqn{\pm 1} cannot be captured by this variant.

The copula correlation \eqn{\rho} is constrained to \eqn{(-0.97, 0.97)}
via \code{rho = 0.97 * tanh(rho_raw)} to avoid boundary singularity in the
Gaussian copula density. Extremely high correlations near \eqn{\pm 1}
are truncated.

\strong{Margins.} The SEM model currently supports only normal marginal
distributions. Non-normal margins (e.g., exponential, gamma) are not
available; use \code{\link[=dcVar]{dcVar()}}, \code{\link[=dcVar_constant]{dcVar_constant()}}, or \code{\link[=dcVar_hmm]{dcVar_hmm()}} instead.

\strong{Post-estimation.} \code{fitted()} and \code{predict()} are not yet implemented
for SEM models. Use \code{\link[=latent_states]{latent_states()}} to extract estimated latent
trajectories.
}
\note{
This model currently supports normal marginal distributions only.
For non-normal margins, use \code{\link[=dcVar]{dcVar()}}, \code{\link[=dcVar_constant]{dcVar_constant()}}, or \code{\link[=dcVar_hmm]{dcVar_hmm()}}.
}
\seealso{
\code{\link[=latent_states]{latent_states()}} for extracting estimated latent states,
\code{\link[=simulate_dcVar_sem]{simulate_dcVar_sem()}} for data generation.
}
