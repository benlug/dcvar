% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-causal-sem.R
\name{simulate_dcvar_causal_sem}
\alias{simulate_dcvar_causal_sem}
\title{Simulate data from a causal SEM model}
\usage{
simulate_dcvar_causal_sem(
  n = 400,
  model = "latent_covariate",
  parameters = list(),
  thresholds = NULL,
  seed = NULL,
  target_weights = c(0.5, 0.5),
  parameter_prior = FALSE,
  priors = list()
)
}
\arguments{
\item{n}{Total number of persons, or two group sizes. A total uses groups
of equal size when possible. Each group needs at least one person.}

\item{model}{A causal SEM model name. See \code{\link[=prepare_causal_sem_data]{prepare_causal_sem_data()}}.}

\item{parameters}{Named list of parameter values. For the first two models,
use length-two vectors \code{mu_v}, \code{sigma_v}, \code{alpha}, \code{beta}, and \code{sigma_y}.
For mediation, use 2 by 2 matrices \code{mu_q}, \code{sigma_q}, \code{B}, and \code{D}.
Also use length-two vectors \code{rho_q}, \code{alpha_m}, \code{alpha_y}, \code{d},
\code{sigma_m}, and \code{sigma_y}. Matrix rows identify control and treatment.
Matrix columns identify the mediator baseline and outcome baseline.
All models accept \code{lambda} of length three and \code{item_sd} of size 2 by 3.
An explicit value must agree with each identification restriction.}

\item{thresholds}{A list of three threshold vectors, or one vector for
all items. Each vector has two to four strictly increasing values.
Alternatively, set \code{threshold_1}, \code{threshold_2}, and \code{threshold_3} in
\code{parameters}. The default is \code{c(-0.7, 0.7)} for each item.}

\item{seed}{Random seed.}

\item{target_weights}{Target population weights in control, treatment order.
A sum within \code{1e-8} of one is accepted. The weights are then divided by
their sum to remove roundoff.}

\item{parameter_prior}{Logical. Draw parameters from the default model prior.
This option uses three categories for each item. To use other category
counts, supply a full parameter draw through \code{parameters}.}

\item{priors}{Prior settings for parameter draws. See \code{\link[=prepare_causal_sem_data]{prepare_causal_sem_data()}}.}
}
\value{
A list with \code{data}, \code{parameters}, \code{effects}, \code{args}, \code{roles}, and
\code{latent}. The last element contains the true person factors. Use
\code{do.call(dcvar_causal_sem, sim$args)} to fit the returned data.
Prior draws can produce a constant indicator. Data preparation reports
this case as an error. The simulator does not replace such a draw.
}
\description{
Generate independent persons with one latent factor and three ordinal
indicators. All parameters use the identified model scale. The first
loading is one. The control factor mean is zero. Each control item SD
is one. The returned fit arguments turn off standardization.
}
