on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

name: R-CMD-check

permissions: read-all

jobs:
  R-CMD-check:
    runs-on: ${{ matrix.config.os }}

    name: ${{ matrix.config.os }} (${{ matrix.config.r }})

    # Linux hosted runners repeatedly terminate during the full Stan-backed
    # test suite. Keep the push matrix CRAN-like, but skip backend sampling on
    # Ubuntu and run those tests in the opt-in Backend Stan tests workflow.
    strategy:
      fail-fast: false
      matrix:
        config:
          - {os: macos-latest, r: 'release'}
          - {os: windows-latest, r: 'release'}
          - {os: ubuntu-latest, r: 'devel', http-user-agent: 'release'}
          - {os: ubuntu-latest, r: 'release'}

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes
      DCVAR_SKIP_STAN_TESTS: ${{ matrix.config.os == 'ubuntu-latest' && 'true' || 'false' }}

    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.config.r }}
          http-user-agent: ${{ matrix.config.http-user-agent }}
          use-public-rspm: true

      # Keep the shared matrix on hard dependencies plus explicit test/vignette
      # packages. Optional CmdStan tooling lives in backend-stan-tests.yaml.
      #
      # DESCRIPTION lists the Stan r-universe in Additional_repositories (for
      # cmdstanr). pak reads that field and offers development rstan/StanHeaders
      # builds (2.36.0.9000) whose version outranks CRAN's 2.32.x, so it prefers
      # them even with a `cran::` source ref. Those dev builds need a newer
      # RcppEigen than CRAN ships, so the Linux source build of rstan fails
      # (e.g. "'plain_enum_min' is not a member of 'Eigen::internal'"). Pin the
      # exact current CRAN versions (which the r-universe does not offer) so pak
      # is forced onto the consistent CRAN trio. Bump or drop these once CRAN's
      # StanHeaders catches up to the newer RcppEigen.
      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          dependencies: 'NA'
          extra-packages: |
            any::BH
            cran::RcppEigen
            StanHeaders@2.32.10
            rstan@2.32.7
            any::testthat
            any::knitr
            any::rmarkdown
            any::tibble
            any::sn
            any::rcmdcheck

      - name: Install qpdf (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y qpdf

      - name: Install qpdf (macOS)
        if: runner.os == 'macOS'
        run: brew install qpdf

      - name: Install qpdf (Windows)
        if: runner.os == 'Windows'
        run: choco install qpdf --no-progress

      - uses: r-lib/actions/check-r-package@v2
        env:
          _R_CHECK_FORCE_SUGGESTS_: false
        with:
          # Keep check artifacts for one diagnostic rerun so platform-specific
          # warnings/errors can be inspected from the uploaded 00check.log.
          upload-results: true
          upload-snapshots: true
          build_args: 'c("--no-manual")'
