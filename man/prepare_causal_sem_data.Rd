% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare-causal-sem.R
\name{prepare_causal_sem_data}
\alias{prepare_causal_sem_data}
\title{Prepare data for a causal SEM model}
\usage{
prepare_causal_sem_data(
  data,
  model,
  treatment,
  control = 0,
  treated = 1,
  outcome,
  covariate = NULL,
  mediator = NULL,
  mediator_baseline = NULL,
  outcome_baseline = NULL,
  indicators,
  target_weights = c(0.5, 0.5),
  standardize = TRUE,
  priors = list(),
  prior_only = FALSE
)
}
\arguments{
\item{data}{A data frame with complete observations for all model columns.}

\item{model}{One of \code{"latent_covariate"}, \code{"latent_outcome"},
\code{"latent_mediator"}, or \code{"latent_mediator_baseline"}.}

\item{treatment}{Name of the column that identifies the two groups.}

\item{control, treated}{Distinct values that identify the control and
treatment groups.}

\item{outcome}{Name of the outcome role.}

\item{covariate}{Name of the covariate role for the first two models.}

\item{mediator}{Name of the mediator role for the mediation models.}

\item{mediator_baseline, outcome_baseline}{Names of the baseline roles
for the mediation models.}

\item{indicators}{A named list with one element. Its name is the latent
role name. Its value names three ordered factor columns. Each column
must declare three to five levels.}

\item{target_weights}{Numeric vector with control and treatment weights.
The values must be non-negative and sum to one.
A sum within \code{1e-8} of one is accepted. The weights are then divided by
their sum to remove roundoff.}

\item{standardize}{Logical. Standardize manifest continuous variables
with their pooled sample mean and sample SD.}

\item{priors}{Named list of prior settings. Supported names are \code{beta_sd},
\code{intercept_sd}, \code{scale_sd}, \code{loading_mean}, \code{loading_sd}, \code{threshold_sd},
\code{item_log_sd}, and \code{lkj_shape}. All values except \code{loading_mean} must
be positive. The defaults are 1, 2.5, 1.5, 1, 1, 2.5, 0.5, and 2.}

\item{prior_only}{Logical. Exclude the observed data likelihood when true.}
}
\value{
A list with \code{stan_data} and \code{meta}. The metadata stores roles,
levels, groups, weights, priors, and each role's \code{center} and \code{scale}.
}
\description{
Validate roles and ordinal indicators. Each row describes one person.
This function keeps all rows. It uses both groups to standardize each
manifest continuous variable when requested.
}
