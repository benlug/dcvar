% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-sem.R
\name{simulate_dcvar_sem}
\alias{simulate_dcvar_sem}
\title{Simulate data from a SEM copula VAR(1) model}
\usage{
simulate_dcvar_sem(
  T = 200,
  J = 3,
  lambda = rep(sqrt(0.8), 3),
  sigma_e = sqrt(0.2),
  Phi = matrix(c(0.5, 0.15, 0.15, 0.3), 2, 2),
  mu = c(0, 0),
  sigma = c(1, 1),
  rho = 0.3,
  burnin = 0,
  seed = NULL
)
}
\arguments{
\item{T}{Number of time points.}

\item{J}{Number of indicators per latent variable.}

\item{lambda}{Numeric vector of length J with factor loadings.}

\item{sigma_e}{Measurement error SD (scalar).}

\item{Phi}{2x2 VAR coefficient matrix.}

\item{mu}{Length-2 intercept vector.}

\item{sigma}{Length-2 innovation SD vector.}

\item{rho}{Copula correlation.}

\item{burnin}{Retained for backward compatibility but ignored. Default \code{0}
keeps the default simulation path aligned with the fitted SEM model,
which conditions on \code{x_0 = 0} and treats the first returned state as
observed rather than drawn after a burn-in period.}

\item{seed}{Random seed for reproducibility.}
}
\value{
A named list with:
\itemize{
\item \code{data}: data frame with columns \code{time}, \code{y1_1}, ..., \code{y1_J},
\code{y2_1}, ..., \code{y2_J}
\item \code{true_params}: list of true parameter values
\item \code{latent_states}: T x 2 matrix of true latent states
\item \code{innovations}: T x 2 matrix of true innovations
}
}
\description{
Generates indicator-level time series data from a latent VAR(1) process
with Gaussian copula dependence and a fixed measurement model.
}
