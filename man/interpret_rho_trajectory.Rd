% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpret.R
\name{interpret_rho_trajectory}
\alias{interpret_rho_trajectory}
\title{Interpret a rho trajectory in clinical terms}
\usage{
interpret_rho_trajectory(
  object,
  threshold = 0.1,
  strength_breaks = .default_strength_breaks,
  magnitude_breaks = .default_magnitude_breaks,
  fluctuation_threshold = 0.3,
  ...
)
}
\arguments{
\item{object}{A fitted model object (\code{dcvar_fit}, \code{dcvar_hmm_fit}, or
\code{dcvar_constant_fit}).}

\item{threshold}{Minimum absolute change in posterior-mean rho to be
considered "meaningful" (default: 0.1).}

\item{strength_breaks}{Named numeric vector of thresholds for classifying
correlation strength (default: \code{c(strong = 0.7, moderate = 0.4, weak = 0.2)}). Values above the highest threshold are "strong", etc.}

\item{magnitude_breaks}{Named numeric vector of thresholds for classifying
the magnitude of trajectory range (default: \code{c(large = 0.5, moderate = 0.3, small = 0.1)}).}

\item{fluctuation_threshold}{Proportion of sign changes in first differences
to flag "substantial fluctuation" (default: 0.3).}

\item{...}{Additional arguments (unused).}
}
\value{
A character string with the interpretation (invisibly). The
interpretation is also printed to the console.
}
\description{
Generates a human-readable interpretation of the estimated rho trajectory,
describing the overall trend, magnitude of change, and key features.
}
\examples{
\dontrun{
sim <- simulate_dcvar(
  n_time = 12,
  rho_trajectory = rho_decreasing(12),
  seed = 1
)
fit <- dcvar(
  sim$Y_df,
  vars = c("y1", "y2"),
  chains = 1,
  iter_warmup = 10,
  iter_sampling = 10,
  refresh = 0,
  seed = 1
)
interpret_rho_trajectory(fit)
}
}
