% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{hmm_state_params}
\alias{hmm_state_params}
\alias{hmm_state_params.default}
\alias{hmm_state_params.dcvar_hmm_fit}
\title{Extract per-state parameters from a Markov-switching HMM fit}
\usage{
hmm_state_params(object, ...)

\method{hmm_state_params}{default}(object, ...)

\method{hmm_state_params}{dcvar_hmm_fit}(object, ...)
}
\arguments{
\item{object}{A \code{dcvar_hmm_fit} object.}

\item{...}{Additional arguments (unused).}
}
\value{
A list with \code{families} (a K x D character matrix), \code{rho_state}
(length-K), \code{mu} (a K x D matrix of posterior means), \code{Phi} (a length-K list
of D x D posterior-mean coefficient matrices), and \code{scales} (a data frame of
per-state margin scale/shape posterior means; shared scales are tiled across
states).
}
\description{
Assembles the state-specific VAR parameters of a switching \code{dcvar_hmm()} fit
into a per-state view: the intercepts, the effective VAR(1) coefficient matrix
(\code{Phi_base} plus the per-state deviations on the switching coefficients), the
margin scale/shape parameters, and the per-state marginal family labels. For
components that are NOT state-specific the shared estimate is tiled across all
states, so the return shape does not depend on which components switch.
}
\seealso{
\code{\link[=hmm_states]{hmm_states()}}, \code{\link[=var_params]{var_params()}}
}
