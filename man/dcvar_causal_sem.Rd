% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-causal-sem.R
\name{dcvar_causal_sem}
\alias{dcvar_causal_sem}
\title{Fit a Bayesian causal SEM with ordinal indicators}
\usage{
dcvar_causal_sem(
  data,
  model,
  treatment,
  control = 0,
  treated = 1,
  outcome,
  covariate = NULL,
  mediator = NULL,
  mediator_baseline = NULL,
  outcome_baseline = NULL,
  indicators,
  target_weights = c(0.5, 0.5),
  standardize = TRUE,
  priors = list(),
  prior_only = FALSE,
  chains = 4,
  iter_warmup = 1000,
  iter_sampling = 1000,
  adapt_delta = 0.95,
  max_treedepth = 12,
  seed = NULL,
  cores = NULL,
  refresh = 100,
  init = NULL,
  backend = getOption("dcvar.backend", "auto"),
  ...
)
}
\arguments{
\item{data}{A data frame with complete observations for all model columns.}

\item{model}{One of \code{"latent_covariate"}, \code{"latent_outcome"},
\code{"latent_mediator"}, or \code{"latent_mediator_baseline"}.}

\item{treatment}{Name of the column that identifies the two groups.}

\item{control, treated}{Distinct values that identify the control and
treatment groups.}

\item{outcome}{Name of the outcome role.}

\item{covariate}{Name of the covariate role for the first two models.}

\item{mediator}{Name of the mediator role for the mediation models.}

\item{mediator_baseline, outcome_baseline}{Names of the baseline roles
for the mediation models.}

\item{indicators}{A named list with one element. Its name is the latent
role name. Its value names three ordered factor columns. Each column
must declare three to five levels.}

\item{target_weights}{Numeric vector with control and treatment weights.
The values must be non-negative and sum to one.
A sum within \code{1e-8} of one is accepted. The weights are then divided by
their sum to remove roundoff.}

\item{standardize}{Logical. Standardize manifest continuous variables
with their pooled sample mean and sample SD.}

\item{priors}{Named list of prior settings. Supported names are \code{beta_sd},
\code{intercept_sd}, \code{scale_sd}, \code{loading_mean}, \code{loading_sd}, \code{threshold_sd},
\code{item_log_sd}, and \code{lkj_shape}. All values except \code{loading_mean} must
be positive. The defaults are 1, 2.5, 1.5, 1, 1, 2.5, 0.5, and 2.}

\item{prior_only}{Logical. Exclude the observed data likelihood when true.}

\item{chains}{Number of MCMC chains.}

\item{iter_warmup}{Number of warmup iterations per chain.}

\item{iter_sampling}{Number of retained iterations per chain.}

\item{adapt_delta}{Target acceptance probability.}

\item{max_treedepth}{Maximum tree depth.}

\item{seed}{Optional integer seed.}

\item{cores}{Number of parallel chains. \code{NULL} uses the package default.}

\item{refresh}{Sampler progress interval. Use zero to suppress progress.}

\item{init}{Initial values accepted by the selected backend. \code{NULL} uses
small random starts and ordered thresholds.}

\item{backend}{Sampling backend: \code{"auto"}, \code{"rstan"}, or \code{"cmdstanr"}.
\code{"auto"} uses RStan.}

\item{...}{Additional arguments for the selected sampler.}
}
\value{
A \code{dcvar_causal_sem_fit} object. It contains the backend fit, Stan
data, priors, roles, group labels, scales, and sampling settings.
}
\description{
Fits one of four models from the SEM causal effects study. Each model has
two treatment groups and one factor with three ordinal indicators. The
models use normal structural errors and an ordinal probit measurement model.
}
\details{
The first loading is one. The factor mean in the control group is zero.
Item residual standard deviations are one in the control group and free in
the treated group. Loadings and thresholds are shared across groups.
Endogenous factor variances follow from their regression and residual
variance. The factor scale does not require known simulation parameters.
The sampler conditions person factors on continuous outcomes when present.
This exact normal factorization reduces dependence during sampling. The
ordinal likelihood then updates these factors. The fit records the factor
parameterization in \code{sampling$factor_parameterization}.

Treatment slopes and structural variances can differ across groups.
\code{\link[=causal_effects]{causal_effects()}} computes effects for each joint posterior draw. The
direct effect follows the paper's adjustment at the pooled mediator mean.
It is not a natural direct effect. A causal interpretation requires the
assumptions that identify the effects in the study design.

\code{prior_only = TRUE} omits all observed likelihood terms. Predictive draws
still condition on supplied manifest predictors where the model uses them.
Use \code{\link[=simulate_dcvar_causal_sem]{simulate_dcvar_causal_sem()}} with \code{parameter_prior = TRUE} to generate
a complete data set from the prior.
}
\examples{
sim <- simulate_dcvar_causal_sem(n = 50, model = "latent_covariate", seed = 42)
prepared <- do.call(prepare_causal_sem_data, sim$args)
\dontrun{
fit <- do.call(dcvar_causal_sem, c(sim$args, list(seed = 42)))
causal_effects(fit)
}
}
\seealso{
\code{\link[=simulate_dcvar_causal_sem]{simulate_dcvar_causal_sem()}}, \code{\link[=causal_effects]{causal_effects()}},
\code{\link[=prepare_causal_sem_data]{prepare_causal_sem_data()}}, \code{\link[=dcvar_diagnostics]{dcvar_diagnostics()}}
}
