% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal-effects.R
\name{causal_effects}
\alias{causal_effects}
\alias{causal_effects.default}
\alias{causal_effects.dcvar_causal_sem_fit}
\title{Extract causal effects from a Bayesian SEM}
\usage{
causal_effects(object, ...)

\method{causal_effects}{default}(object, ...)

\method{causal_effects}{dcvar_causal_sem_fit}(
  object,
  summary = TRUE,
  level = 0.95,
  target_weights = NULL,
  covariate_values = NULL,
  scale = c("response", "model"),
  ...
)
}
\arguments{
\item{object}{A \code{dcvar_causal_sem_fit} object.}

\item{...}{Additional arguments for methods.}

\item{summary}{Return a summary if \code{TRUE}. Otherwise return posterior draws.}

\item{level}{Probability covered by the central credible interval.}

\item{target_weights}{Optional weights for the control and treated groups,
in that order. Two nonnegative values must sum to one. \code{NULL} uses the fit.}

\item{covariate_values}{Optional finite values for conditional treatment
effects in the latent covariate or latent outcome model.}

\item{scale}{\code{"response"} returns effects in the input outcome units and
takes covariate values in input units. \code{"model"} uses the internal scale.
A latent variable always uses its identified factor scale.}
}
\value{
A data frame with effect summaries, or a \code{posterior::draws_array}
when \code{summary = FALSE}. Attributes record the scale and target weights.
}
\description{
Computes effects from each joint posterior draw. The default target assigns
equal weight to both groups. The fit records any other chosen weights.
}
\details{
For the first two models, ATE is the intercept difference plus the slope
difference times the weighted covariate mean. The interaction is the slope
difference. For mediation, the direct effect adjusts both groups at the
same weighted baseline and mediator means. The total effect integrates the
mediator regression at the weighted baseline mean. The indirect effect is
ATE minus ADE. These effects use the definitions in the SEM study.
}
